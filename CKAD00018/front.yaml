apiVersion: v1
kind: Pod
metadata:
  labels:
    app: front
  name: front
  namespace: ckad00018
spec:
  containers:
  - image: hub.atomgit.com/amd64/nginx
    name: front
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
